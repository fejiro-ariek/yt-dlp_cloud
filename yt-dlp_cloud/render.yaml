services:
  - type: web
    name: yt-dlp-api
    runtime: docker
    dockerfilePath: ./Dockerfile
    plan: starter          # upgrade to 'standard' for larger videos / more RAM
    healthCheckPath: /
    envVars:
      - key: PORT
        value: 8080
